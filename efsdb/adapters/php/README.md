# @efsdb/adapter-php

SvelteKit adapter for EFSDB PHP Runtime.

## Usage

```js
import adapter from '@efsdb/adapter-php';

export default {
  kit: {
    adapter: adapter()
  }
};
```

## Local build

```powershell
bun install
bun run build
```

Run those commands in `efsdb/adapters/php`.
